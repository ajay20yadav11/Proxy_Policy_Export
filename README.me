Using Proxy_Polixy_Export.py now network security engineer can export the policy and url categories into an excel sheet.

Current Cisco WSA (Web Security Appliance) doesn't have a provision to export policy and url categories to excel.

Follow below steps to export from WSA:

1. Login to WSA device with admin crediantials.
2. Navigate to Configuration File and export to a local directory(by default format is XML) where python script is present to work.
3. Run the python scrpt.
4. Now in directory an excel file is present which has Policy and URL categories detail.